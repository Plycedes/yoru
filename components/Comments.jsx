import { View, Text } from "react-native";
import React from "react";

const Comments = ({ isVisible }) => {
    return (
        <View>
            <Text>Comments</Text>
        </View>
    );
};

export default Comments;
